"""Pure helper modules shared across routes.

Phase 2 — Onboarding + Profile + Targets. Modules here MUST NOT import Flask or
PyMongo; they are plain Python so they can be unit-tested in isolation and so
the frontend TS mirror can be reasoned about side-by-side.
"""
