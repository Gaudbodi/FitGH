# Phase 1 Walking Skeleton — runtime dependencies for Flask 3.1 backend.
# All versions per .planning/research/STACK.md and 01-RESEARCH.md.
# Pinned to compatible ranges, NOT exact versions, so pip can pull security patches.

flask>=3.1,<4
gunicorn>=25.1
flask-cors>=5.0
pymongo>=4.13,<5
pydantic>=2.9,<3
# Clerk Python SDK for networkless JWT verification on Flask. The httpx.Request
# quirk (Gotcha G5) is encoded in app/middleware/auth.py.
clerk-backend-api>=5.0.6
# svix for Clerk webhook signature verification on the BFF; the BACKEND only sees
# the post-verify forward, but we depend on svix here too for any future server-only
# webhook handlers.
svix>=1.30
python-dotenv>=1.0
sentry-sdk[flask]>=2.53
httpx>=0.27
