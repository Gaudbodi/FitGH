# Phase 1 Walking Skeleton — dev/test dependencies.
# Installed in CI via 'pip install -r requirements.txt -r requirements-dev.txt'.

-r requirements.txt

pytest>=8.0
pytest-cov>=5.0
ruff>=0.5
# respx mocks httpx in clerk-backend-api JWT tests (Gotcha G5 spec).
respx>=0.21
# mongomock fakes PyMongo for unit tests so we don't need a live Atlas connection.
mongomock>=4.1
# pyjwt + cryptography for constructing valid JWTs in test_me.py
pyjwt[crypto]>=2.8
